.DS_Store
.DS_Store?
._*
.Spotlight-V3
.Trashes
__MACOSX/
*.log
data/apps.json

# Dev-only tooling (see package.json); never shipped in the image. The lockfile
# is committed: it pins what CI installs before the checks and the release build.
node_modules/

# Coverage output from `npm test` in api/ (c8).
coverage/

# Playwright output.
playwright-report/
test-results/
